<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.nurgak.treble</groupId>
  <artifactId>bluetooth-remote</artifactId>
  <version>1.0</version>
  
  <dependencies>
    <!-- 添加 Android 依赖 -->
  </dependencies>
  
  <build>
    <plugins>
      <plugin>
        <groupId>com.simpligility.maven.plugins</groupId>
        <artifactId>android-maven-plugin</artifactId>
        <version>4.6.0</version>
        <extensions>true</extensions>
      </plugin>
    </plugins>
  </build>
</project>
